# 単体テスト仕様書: マスタCSVダウンロード（MST-01起点）

## 1. テスト対象
- 対象機能: マスタCSVダウンロード（マスタ検索一覧 MST-01 起点）
- 対象画面またはAPI: `/master`（マスタ一覧画面）の「CSVダウンロード」ボタン、`GET /api/master/exports/csv`
- 対象ファイル:
  - `src/modules/master/ui/master-export-button.tsx`
  - `src/app/(main)/master/page.tsx`（`exportHref` の組み立て）
  - `src/app/api/master/exports/csv/route.ts`
  - `src/modules/master/service.ts`（`exportMasterCsv`）
  - `src/modules/master/export.ts`（`buildMasterExportCsv` / `buildMasterExportFileName`）
  - `src/modules/master/validation.ts`（`requestMasterExportSchema`）
  - `src/shared/observability/with-route.ts`（実行者情報のログ記録）
- 関連DBテーブル: `Master`, `MasterCategory`（読み取りのみ。更新は行わない）
- 関連設計書: [`docs/specs/02_basic-design/master/30_CSVダウンロード.md`](../../../specs/02_basic-design/master/30_CSVダウンロード.md)（§30.1）

## 2. 前提条件
- 実行環境: ローカル環境（`pnpm dev` で起動した検証用サーバー。本番URL・本番DBには接続しない）
- ログインユーザー: VIEWERロールのユーザー（ロールによる制限が無いことを確認する目的で使用する。`.env.example` の `SEED_VIEWER_LOGIN_ID` / `SEED_VIEWER_PASSWORD` に対応するアカウントを使う。ローカルDBに未投入の場合は投入する）
- 権限: 本機能はロールによる制限を行わない（設計書§30.1.5 手順1「ロールによる制限は行わない」）。未ログインの場合は`UNAUTHORIZED`になる。
- 事前DB状態:
  - マスタ分類（分類A）が1件以上登録済みで、分類Aに属するマスタが2件以上登録済み。
  - 分類Aに属し、マスタコードまたはマスタ内容に「AB」を含むマスタと、含まないマスタが両方登録済み。
- 使用するテストデータ: 分類A（マスタコード `AB01` を含むマスタと含まないマスタの両方を持つ）

## 3. テスト観点
- 正常系: 検索条件なし（全件）でのCSVダウンロード、検索条件（マスタ分類・キーワード）を指定した場合の絞り込み
- 異常系: 未ログインでURLを直接開いた場合の`UNAUTHORIZED`
- 境界値: 対象外（理由は「7. 補足」）
- 権限: VIEWERロールでもダウンロードできる（ロールによる制限が無いことの確認）
- DB更新: なし
- 画面表示: ボタン押下から待機表示なくダウンロードが始まること
- エビデンス取得: ダウンロードされたCSVファイルのヘッダー行・BOM・件数、アプリケーションログの記録内容

## 4. テストケース一覧

| No | 区分 | テストケース名 | 前提条件 | 操作手順 | 期待結果 | エビデンス |
|---|---|---|---|---|---|---|
| TC-001 | 正常系/権限 | 全件CSVダウンロード（VIEWERロール） | 分類Aにマスタが2件以上登録済み。VIEWERロールのユーザーでログイン済み。 | `/master` を開く。マスタ分類プルダウンで「すべて」を選択し、マスタ文字列は空欄のまま「検索」ボタンをクリックする。「CSVダウンロード」ボタンをクリックする。 | クリック後、待機表示なくファイル `master_YYYYMMDDHHmmss.csv`（14桁の日時）がダウンロードされる。ダウンロードしたファイルの先頭にBOM（U+FEFF）が付いている。ヘッダー行が「マスタ分類コード,マスタ分類名,マスタID,マスタコード,マスタ内容,登録日時,登録者,最終更新日時,最終更新者」の順で並ぶ。データ行の件数が画面の検索結果件数と一致する。VIEWERロールでもボタンが非活性にならず、ダウンロードが拒否されない。 | 001_CSVダウンロード全件_VIEWER.png、ダウンロードしたCSVファイル |
| TC-002 | 正常系 | 検索条件を指定したCSVダウンロード | 分類Aに、マスタコードまたはマスタ内容に「AB」を含むマスタと含まないマスタが両方登録済み。 | `/master` を開く。マスタ分類プルダウンで分類Aを選択し、マスタ文字列に `AB` と入力して「検索」ボタンをクリックする。「CSVダウンロード」ボタンをクリックする。 | ダウンロードされたCSVのデータ行が、画面の検索結果（分類Aに属し「AB」を含むマスタ）と一致する件数・内容になる。分類Aに属さない、または「AB」を含まないマスタは含まれない。画面が2ページ目以降であっても、条件に一致する全件が出力される（1ページ分に限定されない）。 | 002_CSVダウンロード検索条件指定.png、ダウンロードしたCSVファイル |
| TC-003 | 異常系 | 未ログインでURLを直接開く | ログアウト状態。 | ブラウザーで `/api/master/exports/csv` を直接開く。 | HTTPステータス401が返り、応答本文が `{"error":{"code":"UNAUTHORIZED",...}}` の形になる（`/api` はmiddlewareの認証ガード対象外のため、ログイン画面へのリダイレクトではなくRoute Handlerが直接エラー応答を返す）。CSVはダウンロードされない。 | 003_未ログインアクセス.png |
| TC-004 | 画面表示/エビデンス取得 | 実行者情報のログ記録 | TC-001の操作を行う直前から `pnpm dev` のコンソール出力を確認できる状態にしておく。 | TC-001の操作（CSVダウンロード）を実行する。 | `pnpm dev` のコンソールに `✓ master.export.csv` を含むログ行が出力され、`userId` と `role`（`VIEWER`）がログイン中のVIEWERユーザーの値と一致する。 | 004_CSVダウンロードログ記録.txt（コンソール出力の該当行を転記） |

## 5. DB確認内容

本機能は `Master` / `MasterCategory` テーブルを読み取るだけで、更新・追加・削除は行わない。全テストケースでDB変化はない。

| No | テーブル | 条件 | カラム | 期待値 |
|---|---|---|---|---|
| TC-001〜TC-004 | 該当なし（DB変化なし） | - | - | - |

## 6. 未確定事項
- 無し

## 7. 補足
- ボタンの活性・非活性制御と非活性理由の表示は [`UT_10_マスタ検索一覧.md`](UT_10_マスタ検索一覧.md)（TC-010）で確認済みのため、本仕様書では対象外とする。
- 件数上限（`MASTER_EXPORT_MAX_ROWS` = 10,000件）超過時の `MASTER_EXPORT_LIMIT_EXCEEDED` は、10,000件超のテストデータ投入が現実的でないため、[`UT_10_マスタ検索一覧.md`](UT_10_マスタ検索一覧.md)と同じ理由で本仕様書の対象外とした。
- CSVの列の書式（日時形式・null時の空欄・BOM・CRLF・引用符のエスケープ）は `src/modules/master/export.test.ts`（Vitest）で担保済みのため、本仕様書のTC-001ではヘッダー行と件数の確認に留める。
- テスト観点チェックリストのうち、以下は本機能の性質上対象外とした。
  - 最大文字数・重複・存在しないID・DB制約違反: 本機能は出力専用の読み取り処理であり、登録・更新操作を持たないため対象外。
  - 未入力: 検索条件（マスタ分類・マスタ文字列）はいずれも任意項目であり、未指定時（全件対象）はTC-001で確認している。
