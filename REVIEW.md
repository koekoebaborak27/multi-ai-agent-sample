# REVIEW.md — コミット / PR レビュー観点

> 本テンプレートのレビュー観点（正本）。**コミット単位**と**PR単位**の両方をカバーする。
> 方針の正本は `AGENTS.md`（および `src/AGENTS.md` / `prisma/AGENTS.md`）。本書はレビュー手順に絞る。

## 1. コミット単位の観点

- [ ] **1 コミット = 1 つの論理的変更**になっているか（無関係な変更を混在させない）
- [ ] その**コミット単体でビルド/型/lint/テストが通る**状態か（壊れた中間コミットを残さない）
- [ ] 整形（フォーマット）のみの差分を、機能変更コミットに混ぜていないか（分ける）
- [ ] コミットメッセージが規約どおりか
  - 件名: 何をしたかを簡潔に（命令形・現在形）。必要なら種別プレフィックス（`feat:` / `fix:` / `docs:` 等）
  - 本文: **なぜ**その変更が必要か。対象 PBI / チケット番号を含める
- [ ] 生成物・一時ファイル・デバッグコード・秘密情報をコミットに含めていないか
- [ ] レビュー可能な粒度か（巨大コミットを避ける）

## 2. PR 単位の観点

- [ ] 変更範囲を把握できているか（PR の目的と差分が一致）
- [ ] 無関係な変更が混ざっていないか（意図しないファイル、整形だけの差分）
- [ ] デバッグ用コード・`console` 出力・コメントアウトした旧コードの残骸がないか
- [ ] 秘密情報（APIキー、トークン、認証情報）が差分に含まれていないか
- [ ] `TODO` / `FIXME` を残したまま出していないか
- [ ] テストコードが必要な箇所に書かれているか
- [ ] PR 説明が規約どおりか（対象 PBI / チケット番号、何を・なぜ・どう変更し、どう検証したか）
- [ ] （E2E 画面テストは開発序盤は不要。導入後はその記載も確認）

## 3. このプロジェクト固有（必ず確認）

> ルール本文は各正本に置く。ここは「確認すべき観点 + 参照先」に留め、規約の二重記載を避ける。

- [ ] **依存方向**: `app → modules → shared` の一方向を守れているか（→ `src/AGENTS.md`）
- [ ] **server-only**: サーバ専用コードにマーカーが付き、クライアントへ混入していないか（→ `src/AGENTS.md`）
- [ ] **DB 物理名規約**: 新規カラムが規約どおりか（→ `prisma/AGENTS.md`）
- [ ] **ログ境界**: 業務コードに `try/catch`・ログがなく、入口ラッパー経由で `throw new AppError(...)` のみか（→ `src/AGENTS.md`）
- [ ] **UI / 一覧**: `DESIGN.md` に従っているか（一覧/テーブル規約を含む → `DESIGN.md`）
